<div class="body">
<div class="subhead">
  <h3>Admin Invoices Index</h3>
</div>

  <section class="All Invoices">
    <% @invoices.each do |invoice| %>
      <li>ID: <%= link_to "#{invoice.id}", "/admin/invoices/#{invoice.id}" %></li>
    <% end %>
  </section>

<%= link_to "Sort A-Z", "/admin/invoices?sort=alphabetical" %>

<%= link_to "Sort By Most Recent", "/admin/invoices?sort=date" %>
</div>